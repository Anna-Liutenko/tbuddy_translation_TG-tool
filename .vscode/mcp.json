{
  "inputs": [
    {
      "type": "promptString",
      "id": "perplexityApiKey",
      "description": "Please enter your Perplexity API Key",
      "password": true
    }
  ],
  "servers": {
    "filesystem": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@vscode/mcp-server-filesystem",  // <--- ИЗМЕНЕНИЕ ЗДЕСЬ
        "${workspaceFolder}"
      ]
    },
    "perplexity": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@vscode/mcp-server-perplexity"     // <--- И ИЗМЕНЕНИЕ ЗДЕСЬ
      ],
      "env": {
        "PERPLEXITY_API_KEY": "${input:perplexityApiKey}"
      }
    }
  }
}
